# Phase 2: Refactoring

Status: Planned. Continue from Phase 1.

- Give each function one responsibility.
- Add docstrings describing inputs and outputs.
- Split code into modules where useful.
- Remove repeated logic.
- Improve the data structures.

Keep an example function call and check that results still match the earlier version.
